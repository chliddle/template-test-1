apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: template-test-1

resources:
- ../../base

patches:
- path: patch-environment.yaml
  target:
    kind: Deployment
    name: template-test-1

# Placeholder digest -- CI overwrites this with the real one via
# `kustomize edit set image` after building and pushing to GHCR.
# Reset to placeholder: the image moved from ghcr.io/.../template-test-1 to
# ghcr.io/.../template-test-1 (dynamic, ghcr.io/${{ github.repository }}),
# a fresh package under a name this repo's own workflow creates, avoiding
# the stale cross-repo package-linkage issue the old name had.
images:
- digest: sha256:9d1d347e6bbd7709cede06c0cfa783f21b3c9d7c0c88690c672b56ad2cd396f8
  name: ghcr.io/chliddle/template-test-1
  newName: ghcr.io/chliddle/template-test-1
