apiVersion: v1
kind: Service
metadata:
  name: template-test-1
  labels:
    app.kubernetes.io/name: template-test-1
spec:
  selector:
    app.kubernetes.io/name: template-test-1
  ports:
    - name: http
      port: 8080
      targetPort: http
